{% extends 'BackOfficeBundle:Default:layout.html.twig' %}

{% block content %}
<main class="ui container">
  <section class="ui raised segment">
    <h1 class="ui horizontal divider header">
      <i class="car icon"></i>
      Edition Voiture
    </h1>

    {{ form_start(edit_form) }}
    {{ form_widget(edit_form) }}
    <input class="ui primary button" type="submit" value="Edition" />
    {{ form_end(edit_form) }}

    <a class="ui primary button" href="{{ path('voiture_index') }}">Retourner à la liste</a>
    {{ form_start(delete_form) }}
    <input class="ui red button" type="submit" value="Supprimer">
    {{ form_end(delete_form) }}
  </section>
</main>
{% endblock %}
